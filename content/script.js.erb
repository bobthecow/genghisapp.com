<%= @items.find{|i| i.identifier == '/script/jquery/'}.compiled_content %>
<%= @items.find{|i| i.identifier == '/script/jquery-shuffle/'}.compiled_content %>
<%= @items.find{|i| i.identifier == '/script/socialite/'}.compiled_content %>
<%= @items.find{|i| i.identifier == '/script/socialite-github/'}.compiled_content %>

jQuery(function($) {
  // shuffle the screenshots
  $('#screenshots').shuffle();

  // load the sellout buttons
  Socialite.load($('#share'));

  $('nav input')
    .keydown(function(e) { $(this).text('gem install genghisapp'); })  // no changey
    .click(function(e) { $(this).select(); });                         // select text on click

  $('#changelog h3').prepend(function() {
    return '<a class="permalink" href="#'+$(this).parent().attr('id')+'">&para;</a>';
  });
});
